#include<iostream>
using namespace std;
int main()
{
cout<<"Inheritance allows developers to create subclass that reuse code declared already in a superclass.Avoiding the duplication of common functionality between several classes by building a class inheritance hierarchy can save developer a considerable amount of time.Similarily placing acommon functionality in a single class, rather than duplicating the codein multiple unrelated classes, help prevent the same erors from appearing in multiple source code files.If errors occur in the common functionality of the superclass,the software developer needs to modify only the super class's"<<endl;
} 
